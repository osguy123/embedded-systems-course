#include <stdint.h> // C99 standard integers
#include "TM4C123.h"

int main(){
    unsigned int counter = 0;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    ++counter;
    
    return 0;
}

